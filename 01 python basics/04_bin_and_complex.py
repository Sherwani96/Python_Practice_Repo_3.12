# complex
complex #Create a complex number from a real part and an optional imaginary part. This is equivalent to (real + imag*1j) where imag defaults to 0.
# it is normally used to represent complex numbers in Python.

#bin 
print(bin(5)) # 0b101 (Return the binary representation of an integer.)
print(int('0b101', 2))
# print(bin(3.3)) # float objects cannot be converted to integers
