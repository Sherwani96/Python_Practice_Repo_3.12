# generators are very helpful when calculating large sets of data
# particularly if we are using long loops and we don't want to store them in memory
# and we don't want calculate everything at the same time
# a lot of libraries in python use generators
# generators are much faster than list
