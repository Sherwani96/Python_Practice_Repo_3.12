# generators in python allows us to generate sequence of values over time
# generator function accepts yield keyword, range is also a generator function

print(list(range(2,101,2)))  # it creates list of even number from 2 till 100
