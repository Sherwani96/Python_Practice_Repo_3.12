# the ability of function is that we can call it multiple times or use it
# we call parameters while defining function, and arguments while calling that function
# both are same just names will change
# parameters, we can pass as many parameters as we can in the parentheses

# parameters
def say_hello(name, emoji):
    print(f'Hello {name} {emoji}')


# arguments
say_hello('Ahmed', '😊')