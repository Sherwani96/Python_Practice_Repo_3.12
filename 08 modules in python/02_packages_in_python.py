# when we make a folder it has a file with .py extension, so file inside the folder becomes package
# a package is a folder containing modules
# a folder is a package but the file inside it is a module
# we can only import a package like the following format
# import foldername.modulename