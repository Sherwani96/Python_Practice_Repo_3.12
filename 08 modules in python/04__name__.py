# when we have bigger projects if we run the module but it was not the one we wanted to run
# we use if __name__ == '__main__'
    # code we wanted to run
if __name__ == '__main.py__':
    print('hello')

# encountered that if not in main module using file name with extension wouldn't let the module to be executed
# while in main module can use without extension
